# Notebooks

Place your Jupyter notebooks in this folder. Example starter notebook:

- `notebooks/01-exploratory.ipynb` — dataset loading and initial EDA.

If using Codespaces, open JupyterLab and create new notebooks; the environment will already have the packages from `environment.yml`.
